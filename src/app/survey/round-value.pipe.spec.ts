import { RoundValuePipe } from './round-value.pipe';

describe('RoundValuePipe', () => {
  it('create an instance', () => {
    const pipe = new RoundValuePipe();
    expect(pipe).toBeTruthy();
  });
});
