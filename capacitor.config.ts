import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.peekerpro.app',
  appName: 'peeker-pro-admin',
  webDir: 'dist/my-app'
};

export default config;
